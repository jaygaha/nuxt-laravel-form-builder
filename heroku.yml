setup:
  addons:
    - plan: heroku-nuxt-mysql
      as: DATABASE
  config:
  // .env vars
build:
  docker:
    nginx-dynamic-forms: docker/nginx/Dockerfile
    backend-dynamic-forms: docker/php/Dockerfile
    mysql-dynamic-forms: docker/mysql/Dockerfile
    frontend-dynamic-forms: docker/node/Dockerfile